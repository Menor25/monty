#include "monty.h"

/**
 * nop_stack -Main entry
 * Description: it does not do anythin
 * @stack: pointer to the top of the stack
 * @line_number: line number of the opcode
 * Return void
 **/
void nop_stack(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
